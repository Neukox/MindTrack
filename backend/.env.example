# --------------------------------
# 🗄️ Banco de Dados PostgreSQL
# --------------------------------
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=mindtrack_db

# URL de conexão completa (usada pelo Prisma)
DATABASE_URL="postgresql://user:password@localhost:9000/nomedobanco?schema=public"

# --------------------------------
# ⚙️ Configurações do Prisma
# --------------------------------
RUN_MIGRATIONS=false  # Defina como 'true' para executar migrações automaticamente na inicialização (preferivel para usar ao rodar containers Docker)

# --------------------------------
# 🔐 Configurações de recuperação de senha
# --------------------------------
RESET_PASSWORD_TOKEN_EXPIRATION_MINUTES=15

# URL do cliente (frontend)
CLIENT_URL=http://localhost:5173

# --------------------------------
# 📧 Configurações de e-mail (SMTP Gmail)
# --------------------------------
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=seu_email@gmail.com
EMAIL_PASS=sua_senha_de_app_aqui
EMAIL_FROM="MindTrack App" <seu_email@gmail.com>

# NOTA:
# - Use a senha de app do Gmail (16 caracteres, sem espaços)
# - Se EMAIL_USER e EMAIL_PASS estiverem vazios,
#   o sistema exibirá o token no console em vez de enviar e-mail

# --------------------------------
# 🔑 Configurações JWT
# --------------------------------
# Token de acesso (curta duração)
JWT_ACCESS_SECRET=sua_chave_jwt_aqui
JWT_ACCESS_EXPIRATION=900000   # 15 minutos (em milissegundos)

# Token de atualização (longa duração)
JWT_REFRESH_SECRET=sua_chave_jwt_refresh_aqui
JWT_REFRESH_EXPIRATION=604800000  # 7 dias (em milissegundos)

# --------------------------------
# Configurações de cookies
# --------------------------------
# Define se o cookie deve ser seguro (true para HTTPS, false para HTTP)
COOKIE_SECURE=false
# Define se o cookie é acessível apenas via HTTP (não acessível via JavaScript)
COOKIE_HTTP_ONLY=true
# Define a política SameSite do cookie (lax, strict, none)
COOKIE_SAME_SITE=lax
